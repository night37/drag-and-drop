import { BibliothequueDirective } from './bibliothequue.directive';

describe('BibliothequueDirective', () => {
  it('should create an instance', () => {
    const directive = new BibliothequueDirective();
    expect(directive).toBeTruthy();
  });
});
